namespace SuperErp.Core
{
    public static class CustomClaimTypes
    {
        public const string DelegationId = nameof(DelegationId);
        public const string StartDate = nameof(StartDate);
    }
}